/*
 *      As we can see packages directory(folder) is inside another directory named OOPS
 * Hence, we initiated the package as OOPS.Packages --> main_directory.sub_directory
 * 
 * Now, we can use/access other classes in the Packages directory by creating an instance if non-static else called statically
 */
package OOPS.Packages;

public class PackMan {
    
}
